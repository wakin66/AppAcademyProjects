require 'tdd'

describe TDD do
    describe "#my_uniq" do
        it "takes an array as a parameter"

        it "raises an error if passed something other than an array"

        it "returns an array with no duplicates"

        it "returns a subarray of the parameter it was passed"
    end
end